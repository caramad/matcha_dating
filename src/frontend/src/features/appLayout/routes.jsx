import { Route } from "react-router-dom";
import AppLayoutPage from "./AppLayoutPage";

const AppLayoutRoutes = [
	<Route key="appLayout" path="/appLayout" element={<AppLayoutPage />} />,
];

export default AppLayoutRoutes;